import React from 'react';
import {action} from '@storybook/addon-actions';

// Components

export default {
  title: 'Widgets/Tornado',
  // component: Button
};

export const normal = () => <div></div>;
