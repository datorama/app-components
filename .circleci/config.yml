version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  test-build:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm build:package
workflows:
  build-and-test:
    jobs:
      - test-build
