import React from 'react';

// Components
import Spinner from '../../packages/core/Spinner'

export default {
  title: 'Core/Spinner',
  component: Spinner
};

export const normal = () => <Spinner />;
