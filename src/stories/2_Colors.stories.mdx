import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Colors } from './Colors';

<Meta title="Colors" />

# Colors

Import `lightTheme` or `darkTheme` and pass it to the `AppTheme` component.

```javascript
// In your styled component
background: ${({ theme }) => theme.a400};
``` 

<Colors />
