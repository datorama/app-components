import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Notes" />

# App Components

<br /><br /><br />


## Pending Deprecation

The following components are pending deprecation and will be removed in next releases. Avoid using them in new features.

`Table`, `IllustratedMessage`, `Ellipsis`, `Draggable`, `Droppable`, `DragDropProvider`, `withToast`, `withSnackbar`, `ToastsProvider`

<br /><br />

## 2.8.0
Range - flag for controlling the range with input field

## 2.7.3
Add optional `rowHeight` prop for Select's options

## 2.7.3
Fix issue with sorting in Grid component.

## 2.7.1
Export search input component

## 2.7.0
Search input component

## 2.6.0
Text highlight hook

## 2.4.2
Fixed import in Search component

## 2.4.1
Export Search component

## 2.4.0
Search component with combo box

## 2.3.0
Select - remove selection is now available by using `isSingleOptionRemovable` flag

## 2.2.3
Select - ignore portal clicks in order to avoid unexpected ClickOut triggering.

## 2.2.2
Select - menu portal dynamic width based on parent width

## 2.2.1
Select - reposition menu portal on resize

## 2.2.0
TextInput - maxLength attribute added

## 2.1.1
AppThemeProvider - fixed circular dependencies & TS wrong types

## 2.1.0
Select - added `usePortalForMenu` flag, which will render the select menu in a portal in order to overcome relative render in a container with `overflow: auto/scroll`

## 2.0.0
AppThemeProvider - a replacement component for AppTheme which also builds the theme.

## 1.16.0
TextInput - onBlur event & defaultValue

## 1.15.2
Range - Add label and value renderers, add innerMax prop, fix buggy onDragEnd when releasing inside the range-component.

## 1.15.1
Export [useUploadFile](?path=/docs/utils-upload-file--page) util hook together with UploadFileProvider

## 1.15.0
Add [Upload File utility component](?path=/docs/utils-upload-file--page)

## 1.14.3
Fix Select bug: hide placeholder when tags are displayed

## 1.14.2
Popover - Fixed click outside handler

## 1.14.1
Popover - Changed trigger ref type to be either HTMLElement | SVGSVGElement

## 1.14.0
Converted the AppTheme to Typescript. Added a Theme type.

## 1.13.0
Added placeholder support to the [Datepicker](?path=/story/core-datepicker--primary) component

## 1.12.2
- Fixed stale-headers issue in [Grid](?path=/story/core-grid--primary) component

## 1.12.1
- Fixed circular dependency in Popover component

## 1.12.0
- Added [useConfirmationModal](?path=/docs/utils-confirmation-modal--page) utility hook

## 1.11.0
- Added generic popover component

## 1.10.1
- Allow the Grid component consumer to pass additional data-columns that will only be searched (and not displayed in the table). This way, the user will can get results for formatted-data, etc.

## 1.10.0
- Added `onRowClick` prop to the `Grid` component

## 1.9.5
- Added optional `headerRenderer` prop in DatePicker: `<DatePicker headerRenderer={({ preset, dateRange, formattedTitle }) => string | ReactNode}>`

## 1.9.4
- Removed source maps from build

## 1.9.3
- Updated the `Grid Layout` breakpoints and and added margin

## 1.9.2
- Added class names to DatePicker's dropdown elements

## 1.9.1
- Fixed `timeout` parameter in notifications config. When set to `Infinity`, the notification will not be cleared automatically
- Return `onClear(id)` & `onClearAll()` callbacks on `useToast()` & `useSnackbar()` hooks usage:

```
 const { addSnackbar, onClear, onClearAll } = useSnackbar();
```

- Return notification id on `addToast` & `addSnackbar` usage, allowing the user to clear a notification programatically:

```
const { addToast, onClear } = useToast();
const id = addToast(config);
...
onClear(id);
```

## 1.9.0
Added svgr loader and a new  `Asset` component.

## 1.8.0
Updated the `select` component - selecting/deselecting all is now using filtered result context.


## 1.7.0
Segmented Button is now typed. It can also accept an input of border radius, overriding the default (4px).

## 1.6.0
Added a new Grid layout containers.

## 1.5.0
Added tooltips capability to segmented button.

## 1.4.1
Added a filtered data prop to the Grid cellRenderer.

## 1.4.0

Added global styles which enable branding SVGs.

## 1.3.7

Fix tokenization in the Grid component (js-search).

## 1.3.6

Fixed issue with the `Grid` component - updating the fuzzy search documents when the headers or data is changing.

## 1.3.5

Added rowIndex prop to the cellRenderer function in the Grid component.

## 1.3.4

Fixed BasicLine tooltip positioning.

## 1.3.3

Added key prop to the cellRenderer function in the Grid component.

## 1.3.2

Added header width and isResizable to the Grid component.

## 1.3.1

Expose BasicLine component.

## 1.3.0

Added a `Grid` component with client search, sorting, column resize, empty state, loading state (skeleton), error state, different rendereres and prep for server sort and server search.

## 1.2.0

Added a new component `BasicLine` - a simple line chart component with tooltip.
