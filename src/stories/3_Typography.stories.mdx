import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Typography } from './Typography';

<Meta title="Typography" />

# Typography

```javascript
// In your styled component - apply a text style
${({ theme }) => theme.text.sm};
``` 

<Typography />
