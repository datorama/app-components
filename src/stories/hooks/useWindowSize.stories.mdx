import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="hooks/useWindowSize" />

# ReactJS Hooks: useWindowSize

````jsx harmony
import React from 'react';
import {useWindowSize} from '@datorama/app-components';

const Comp = () => {
  const {
    innerWidth,
    innerHeight,
    outerWidth,
    outerHeight
  } = useWindowSize();
  
  return <Page/>;
};
````
